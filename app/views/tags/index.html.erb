<h1>Tags</h1>

<div id="tags">
  <% @tags.each do |tag| %>
    <%= render tag %>
    <p>
      <%= link_to "Show this tag", tag %>
    </p>
  <% end %>
  <%= link_to "Create a new tag", new_tag_path %>
  <%= link_to "Back to clients", clients_path %>
</div>
